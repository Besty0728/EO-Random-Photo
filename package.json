{
    "name": "eo-random-photo",
    "version": "1.0.0",
    "description": "Random Photo API for Tencent Cloud EdgeOne Pages",
    "type": "module",
    "scripts": {
        "generate:manifest": "node scripts/generate-manifest.js",
        "build": "npm run generate:manifest",
        "dev": "npm run generate:manifest && npx wrangler pages dev public"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20260103.0",
        "@types/node": "^20.0.0",
        "typescript": "^5.0.0",
        "wrangler": "^3.0.0"
    }
}
